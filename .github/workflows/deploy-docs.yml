name: Deploy Docusaurus Docs (Operating Manual) [disabled]

on:
  # Disable automatic triggers to avoid conflicting with official deploy-pages workflow
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    if: false  # disabled in favor of docs-deploy.yml (actions/deploy-pages)
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use 'Deploy Operations Manual to GitHub Pages' instead."
