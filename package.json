{
  "name": "iQubeBeta-Program",
  "private": true,
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "capture:sidebar": "node scripts/capture-sidebar.js",
    "capture:sidebar:url": "AIGENTZ_URL=http://localhost:3000/dashboard node scripts/capture-sidebar.js"
  },
  "devDependencies": {
    "puppeteer": "^24.22.0"
  }
}
